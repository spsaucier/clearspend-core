spring:
  datasource:
    url: jdbc:postgresql://db:5432/postgres
    username: postgres
    password: docker
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    root: INFO
    com.clearspend.capital: DEBUG
