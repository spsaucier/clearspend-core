package com.clearspend.capital.data.model.enums;

/** Matching the database enum of the same name */
public enum AllocationPermission {
  READ,
  CATEGORIZE,
  LINK_RECEIPTS,
  MANAGE_FUNDS,
  MANAGE_CARDS,
  MANAGE_USERS,
  MANAGE_PERMISSIONS,
  MANAGE_CONNECTIONS,
  VIEW_OWN,
  LINK_BANK_ACCOUNTS
}
